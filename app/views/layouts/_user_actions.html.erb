<% if logged_in? %>
  <p>
    <%= t('.logged_in_as', user: current_user.email) %> |
    <%= link_to t('.admin_integrations'), web_admin_integrations_path %> |
    <%= link_to t('.logout'), web_logout_path %>
  </p>
<% else %>
  <p><%= link_to t('.login'), web_login_path %></p>
<% end %>
